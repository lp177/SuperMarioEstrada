{
  "name": "supermarioestrada",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Super Mario Estrada — a satirical 2D platformer. The greatest rescue that never happened.",
  "scripts": {
    "dev": "vite",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit && vitest run && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "vite": "^7.1.0",
    "vitest": "^4.0.0"
  }
}
